# SPFN Mobile — example cell keyboardForm-close: R5 — close empties the stack whatever the depth and whatever presented it, so the flow is no longer on show
#
# GENERATED FILE — DO NOT EDIT.
#
# generator:       spfn-ui-codegen 0.1.0-dev
# spec:            examples/ui-spec/device-approval.json
# specSha256:      88e5159b5528860daa36d6ebae1f6a6940c8152eb8373bf4cb3656be70599153
# bundleSha256:    29c26160b5b62d3e40f76bbf81785c8b6808c85690fe047c715e3f348801d92c
# contractVersion: 0.10.0
#
# Regenerate with: ./gradlew :ui-codegen:spfnGenerateUi
# Verified by:     ./gradlew :ui-codegen:spfnUiVerify

appId: ${APP_ID}
name: keyboardForm-close
---
- launchApp:
    clearState: true
    arguments:
      SPFN_UI_FIXTURE: "keyboardForm-close"

# The app is up once a screen is drawing its stack readout. That the right FIXTURE
# is installed is proven at the end instead, by the receipt's own name: an app that
# never got the launch argument writes no receipt for this cell at all.
#
# This first wait is the long one. The FIRST launch after an install or a wipe draws
# later than every launch after it, and a cold start that outran this wait is a
# stall reported as a cell failure — cell u14 did exactly that on a wiped emulator
# on 2026-09-02 and passed twice warm. Every later wait stays at 20000.
- extendedWaitUntil:
    visible:
      text: "stack=.*"
    timeout: 45000

- tapOn:
    id: "form.userCode"
- inputText: "ABCD-1234"

- scrollUntilVisible:
    element:
      id: "form.submit"

- assertVisible:
    id: "form.submit"

- tapOn:
    id: "form.submit"

- assertVisible:
    text: "stack=0"

- tapOn:
    id: "example.receipt"

- assertVisible:
    text: "receipt=receipt-keyboardForm-close-.*"
