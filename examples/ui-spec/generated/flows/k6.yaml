# SPFN Mobile — example cell k6: K6 — editing the field clears the refusal under it, so the screen is usable again without the person pressing anything
#
# GENERATED FILE — DO NOT EDIT.
#
# generator:       spfn-ui-codegen 0.1.0-alpha.3
# spec:            examples/ui-spec
# specSha256:      69fbbe100243bdb7d7c98ea11feae130988cff28c517c37bc9bb6942b05023e6
# bundleSha256:    bb0373c2c3e95bcc3923c84a160945e17ca57d5c13fd8122f341f1df181bc658
# contractVersion: 0.13.0
#
# Regenerate with: ./gradlew :ui-codegen:spfnGenerateUi
# Verified by:     ./gradlew :ui-codegen:spfnUiVerify

appId: ${APP_ID}
name: k6
---
- launchApp:
    clearState: true
    arguments:
      SPFN_UI_FIXTURE: "k6"

# The app is up once a screen is drawing its stack readout. That the right FIXTURE
# is installed is proven at the end instead, by the receipt's own name: an app that
# never got the launch argument writes no receipt for this cell at all.
#
# This first wait is the long one. The FIRST launch after an install or a wipe draws
# later than every launch after it, and a cold start that outran this wait is a
# stall reported as a cell failure — cell u14 did exactly that on a wiped emulator
# on 2026-09-02 and passed twice warm. Every later wait stays at 20000.
- extendedWaitUntil:
    visible:
      text: "stack=.*"
    timeout: 45000

- tapOn:
    id: "enterCode.submit"

- extendedWaitUntil:
    visible:
      text: "state=error"
    timeout: 20000

- tapOn:
    id: "enterCode.userCode"

- tapOn:
    id: "enterCode.userCode"
- inputText: "ABCD-1234"

- assertVisible:
    text: "stack=1"

- assertVisible:
    text: "state=idle"

# The assertions are made. What follows only leaves the flow closed, so the
# receipt control on the app's root is reachable on both platforms.
- tapOn:
    id: "enterCode.cancel"

- tapOn:
    id: "example.receipt"

- assertVisible:
    text: "receipt=receipt-k6-.*"
